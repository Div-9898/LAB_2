def greet(name: str) -> str:
    """
    Returns a greeting message.
    """
    return f"Hello, {name}!"

if __name__ == "__main__":
    # Change "World" to your name when testing
    print(greet("✨ from feature branch"))

def greet(name: str) -> str:
    """
    Returns a greeting message.
    """
    return f"Hello, {name}!"

if __name__ == "__main__":
    # Change "World" to your name when testing
    print(greet("👋 from main branch_"))